import 'package:flutter/material.dart';

class SucessStories extends StatelessWidget {
  const SucessStories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}